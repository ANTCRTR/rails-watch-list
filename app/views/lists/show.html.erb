<h2><%= @list.name %></h2>

<% @list.movies.each do |movie| %>
<p><%= movie.title  %></p>
<%end%>

<%= link_to "Create new bookmark", new_list_bookmark_path(@list), class: "btn btn-dark"%>

<%= link_to "Home", lists_path(@list), class: "btn btn-primary"%>
