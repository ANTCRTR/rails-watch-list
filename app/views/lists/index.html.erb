<ul>
  <% @lists.each do |list| %>
    <li>
    <%= list.name %> <%= link_to "See details", list_path(list), class: "btn btn-primary"%>
  </li>
  <br>
  <%end%>
</ul>
<h1> <%= link_to "Create new list", new_list_path(@list) %> </h1>
